# ===========================================
# MedSpa Dashboard Environment Configuration
# ===========================================
# Copy this file to .env.local and fill in your values

# ===========================================
# Authentication (NextAuth.js)
# ===========================================

NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-change-in-production

# Shared password for all users (MVP - use individual passwords in production)
AUTH_PASSWORD=medspa2024

# ===========================================
# OPTION 1: Google Sheets (Recommended for Production)
# ===========================================
# This allows the dashboard to read LIVE data from Google Sheets
# while reps continue logging in their familiar spreadsheets.

# Google Service Account Credentials
# ---------------------------------
# 1. Go to Google Cloud Console: https://console.cloud.google.com/
# 2. Create or select a project
# 3. Enable Google Sheets API: APIs & Services → Library → "Google Sheets API" → Enable
# 4. Create Service Account: IAM & Admin → Service Accounts → Create
#    - Name: "medspa-dashboard-reader"
#    - Grant "Viewer" role
# 5. Create key: Click service account → Keys → Add Key → JSON → Download
# 6. From the JSON file, copy these values:

GOOGLE_SERVICE_ACCOUNT_EMAIL=medspa-dashboard-reader@your-project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Google Sheet IDs
# ----------------
# Find these in your Google Sheets URLs:
# https://docs.google.com/spreadsheets/d/{THIS_IS_THE_SHEET_ID}/edit
#
# IMPORTANT: Share each sheet with the service account email (Viewer access)

BOOKED_CALLS_SHEET_ID=your_booked_calls_sheet_id
SALES_SHEET_ID=your_sales_sheet_id

# Optional: Custom sheet/range names
# ----------------------------------
# By default, reads from monthly sheets (June, July, etc.) for booked calls
# and "New Sale Submission Form  (Resp" for sales.
# Uncomment and customize if your sheets have different names:

# BOOKED_CALLS_RANGE=Sheet1!A:Z
# SALES_RANGE=Form Responses 1!A:Z

# ===========================================
# OPTION 2: Local Excel Files (Development/Offline)
# ===========================================
# Falls back to this if Google Sheets credentials are not set

EXCEL_DATA_PATH=/Users/jj/Documents/MedSpa

# Expected files in the directory:
# - Copy of Booked Calls - IIAM Michael x Hannah.xlsx
# - Copy of New Sale Submission Form  (Responses).xlsx

# ===========================================
# OPTION 3: Demo Data (No Configuration)
# ===========================================
# If neither Google Sheets nor Excel files are available,
# the dashboard will display demo data automatically.
